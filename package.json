{
  "name": "flowery-client",
  "version": "0.0.0",
  "private": true,
  "description": "flowery-client",
  "scripts": {
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "build": "pnpm -r build",
    "web:add": "pnpm -F web add",
    "web:del": "pnpm -F web remove",
    "ui:add": "pnpm -F @packages/ui add",
    "ui:del": "pnpm -F @packages/ui remove",
    "ui:shadcn:add": "cd packages/ui && pnpm dlx shadcn@canary add",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "packageManager": "pnpm@9.15.1",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "prettier": "^3.4.2"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
